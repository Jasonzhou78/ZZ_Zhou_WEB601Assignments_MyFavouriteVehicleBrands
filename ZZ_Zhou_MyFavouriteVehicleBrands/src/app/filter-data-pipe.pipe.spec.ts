import { FilterDataPipePipe } from './filter-data-pipe.pipe';

describe('FilterDataPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDataPipePipe();
    expect(pipe).toBeTruthy();
  });
});
